/**
 *  Internal interface through which a {@link @xeokit/scene!SceneGeometry} can load any updated geometry arrays into a render, if attached.
 *
 * @internal
 */
export interface RendererGeometry {

}