import type {Texture} from "./Texture";

/**
 *  Internal interface through which a {@link Texture} can load property updates into a renderer.
 *
 *  This exists at {@link Texture.rendererTexture} when the {@link SceneModel} has been added
 *  to a {@link @xeokit/viewer!Viewer | Viewer}.
 *
 * @internal
 */
export interface RendererTexture {

}