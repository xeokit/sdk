/**
 * Enumeration of SDK error types.
 */
export enum SDKErrorType {
    InvalidParameter = 1,
    NotFound = 2,
    OutOfMemory = 3,
    Unknown = 4,
    Destroyed,
    InvalidOperation,
    NotSupported,
}