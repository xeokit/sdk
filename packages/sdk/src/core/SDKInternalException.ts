/**
 * Represents an internal SDK exception. These are never thrown to
 * the SDK user, but are used internally to signal unexpected conditions.
 */
export class SDKInternalException extends Error {

}