/**
 * # Schema Materials Panel
 *
 * Floating panel that surfaces the {@link DataModel}'s schemas and
 * types as a list, lets the user pick a procedural PBR painter for
 * each type from a {@link MaterialsPalette}, and applies the user's
 * selections to the matching {@link SceneModel} via
 * {@link applyIFCMaterials}.
 *
 * @module demo/schemaMaterialsPanel
 */
export * from "./SchemaMaterialsPanel";
