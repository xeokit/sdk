/**
 * ViewsPanel — floating, draggable panel that lists every
 * {@link "../../viewer".View | View} mounted on a
 * {@link "../../viewer".Viewer | Viewer}, with per-row close
 * buttons and a "New View" footer button.
 *
 * Sister widget to the other floating demo panels — same
 * floating chrome and drag / persistence mechanics.
 *
 * @module demo/viewsPanel
 */

export {ViewsPanel} from "./ViewsPanel";
export type {ViewsPanelParams} from "./ViewsPanelParams";
