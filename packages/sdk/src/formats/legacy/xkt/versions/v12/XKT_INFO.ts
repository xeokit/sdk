/**
 * XKT v12 file-format constants.
 *
 * The exporter writes the uncompressed offset-table container (high version
 * bit clear), matching `xeokit-convert`'s `writeXKTModelToArrayBufferUncompressed`.
 */
export const XKT_INFO = {
  xktVersion: 12,
};
