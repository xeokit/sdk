/**
 * XGF file-format constants.
 *
 * The format carries geometry (positions, normals, UVs,
 * per-vertex colours, indices, edges, AABBs, matrices), 3D Gaussian Splatting
 * geometry, PBR materials with textures (+ colour-space encoding), per-material
 * `triplanarScale`, meshes and objects — see {@link XGFData_v1} for the layout.
 */
export const XGF_INFO = {
  xgfVersion: 1
};
