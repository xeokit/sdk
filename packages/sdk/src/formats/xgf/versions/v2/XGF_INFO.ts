/**
 * XGF v2 file-format constants.
 *
 * v2 adds stable geometry IDs and per-mesh external geometry/material IDs so
 * XGF files can be split into reusable asset libraries and mesh/object chunks.
 */
export const XGF_INFO = {
  xgfVersion: 2
};
