/**
 * XGF v2 file-format constants.
 *
 * v2 adds materials with full PBR + alpha attributes, per-geometry
 * normals + UVs, and a texture array — see {@link XGFData_v2} for the
 * full layout.
 */
export const XGF_INFO = {
  xgfVersion: 2
};
