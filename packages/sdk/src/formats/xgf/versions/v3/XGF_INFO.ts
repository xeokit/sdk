/**
 * XGF v3 file-format constants.
 *
 * v3 adds 3D Gaussian Splatting geometry on top of v2: per-splat scales
 * and rotation quaternions, plus the `GaussianSplatsPrimitive` primitive
 * type — see {@link XGFData_v3} for the full layout.
 */
export const XGF_INFO = {
  xgfVersion: 3
};
