/**
 * Floating dialog widgets the demo composes on top of the panel
 * chrome — currently a progress dialog for long-running loader
 * calls; future entries (confirm dialogs, prompt dialogs, etc.)
 * land here too.
 *
 * @module demo/dialogs
 */
export * from "./LoaderProgressDialog";
