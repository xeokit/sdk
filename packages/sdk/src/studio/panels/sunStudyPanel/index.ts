/**
 * Floating panel for editing a `SunStudy`'s geo-location + time
 * cursor, with optional play / pause / day-or-year mode controls
 * when an `AnnualSunPlayer` is wired in.
 *
 */
export * from "./SunStudyPanel";
