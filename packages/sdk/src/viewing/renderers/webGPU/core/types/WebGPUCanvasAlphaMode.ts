/**
 * WebGPU canvas alpha mode.
 *
 * `opaque` configures an opaque swap-chain target. `premultiplied` configures
 * a transparent target compatible with premultiplied alpha compositing.
 */
export type WebGPUCanvasAlphaMode = "opaque" | "premultiplied";
