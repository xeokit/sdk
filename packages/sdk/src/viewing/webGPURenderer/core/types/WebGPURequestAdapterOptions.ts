/**
 * Options passed to `navigator.gpu.requestAdapter`.
 *
 * This is intentionally structural so the SDK does not require WebGPU DOM
 * typings in every TypeScript environment.
 */
export type WebGPURequestAdapterOptions = object;
