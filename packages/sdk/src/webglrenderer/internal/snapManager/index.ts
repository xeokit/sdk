/**
 * Snap-pick driver — runs the snap-init + snap-vertex + snap-edge
 * passes against a small framebuffer centred on the cursor and
 * scans the read-back for the nearest snap target.
 *
 * @internal
 */
export * from "./SnapManager";
