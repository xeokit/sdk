import { View } from "../viewer";
/**
 * Handles camera updates on each "tick" that were scheduled by the various controllers.
 *
 * @private
 */
export declare class CameraUpdater {
    #private;
    constructor(view: View, controllers: any, configs: any, states: any, updates: any);
    destroy(): void;
}
//# sourceMappingURL=CameraUpdater.d.ts.map