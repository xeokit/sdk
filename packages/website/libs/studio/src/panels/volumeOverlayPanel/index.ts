/**
 * Floating Studio panel for slicing a {@link VoxelGrid} —
 * axis-selector, in-axis position slider, colormap picker, value
 * range, opacity, and a colourbar legend. Owns the lifetime of the
 * slice SceneModel it builds.
 *
 */
export * from "./VolumeOverlayPanel";
