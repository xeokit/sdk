# Render Benchmarks

Browser-driven probes, renderer comparisons, and profiling scripts live here.

- `webgpu/` contains renderer and render-path benchmarks or comparisons.
- `xgf-streaming/` contains streaming and LOD-focused benchmarks.

These scripts may start local HTTP servers and drive browser automation. They
are not part of the normal website build.
