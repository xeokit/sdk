import {BuildableModel} from "@xeokit/core/components";

/**
 * Loads XKT file data into a {@link BuildableModel}.
 *
 * @param xktData
 * @param readableModel
 * @param options
 */
export function loadXKT(xktData: ArrayBuffer, buildableModel:BuildableModel, options?:{}) {

}