import {ReadableModel} from "@xeokit/core/components";

/**
 * Saves a {@link ReadableModel} to XKT file data.
 *
 * @param readableModel
 * @param options
 */
export function saveXKT(readableModel:ReadableModel, options?:{}) : ArrayBuffer {
    return null;
}